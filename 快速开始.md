# YouTube视频研究工作流 - 快速开始指南

这是一个基于认知敏捷法(42COG)的YouTube视频研究工作流项目。本指南将带您在5分钟内完成从零开始的完整配置和首次运行。

## 第一步：环境检查

```bash
# 检查Python版本（需要3.11+）
python3 --version

# 检查Node.js版本（需要v20+）
node --version

# 检查CC-Switch状态（MCP服务器管理）
ps aux | grep cc-switch | grep -v grep

# 检查启用的MCP服务器
sqlite3 /Users/su/.cc-switch/cc-switch.db "SELECT name FROM mcp_servers WHERE enabled_claude = 1;"

# 检查Git版本
git --version
```

## 第二步：安装依赖

```bash
cd "/Users/su/Downloads/3d_games/youtube 视频的最小故事"

# 方式1：使用pip
pip install -r requirements.txt

# 方式2：使用uv（推荐，更快）
uv pip install -r requirements.txt

# 方式3：使用Bun（最快）
bun install -r requirements.txt
```

## 第三步：配置MCP环境

```bash
# 检查CC-Switch MCP服务器状态
ps aux | grep cc-switch

# 检查启用的MCP服务器
sqlite3 /Users/su/.cc-switch/cc-switch.db "SELECT name FROM mcp_servers WHERE enabled_claude = 1;"

# 可选：配置环境变量
cp .env.example .env
```

## 第四步：Git版本控制

```bash
cd "/Users/su/Downloads/3d_games/youtube 视频的最小故事"

# 初始化Git仓库
git init

# 配置用户信息
git config user.name "Your Name"
git config user.email "your.email@example.com"

# 添加所有文件
git add .

# 创建首次提交
git commit -m "feat: 完整实现YouTube视频研究工作流

主要功能:
- 基于MCP fetch的数据收集方案
- 双轨研究系统（长期模式+短期动态）
- 完整42COG框架实现
- 5个示例脚本
- 动态追踪模块"

# 创建版本标签
git tag -a v0.1.0 -m "Release v0.1.0 - 项目框架搭建完成"

# 推送到GitHub（如果已有远程仓库）
git remote add origin https://github.com/username/youtube-video-research-workflow.git
git push -u origin main
git push origin v0.1.0
```

📚 **完整Git工作流指南**：详见 [`GIT_WORKFLOW.md`](./GIT_WORKFLOW.md)

## 第五步：运行快速示例

```bash
# 运行完整的三事件工作流示例
python3 run.py quick
```

**预期输出**：
```
============================================================
YouTube视频研究工作流 - 快速开始示例
============================================================

📋 步骤1：配置初始化
✅ 配置文件加载完成

🔍 步骤2：事件1 - 数据收集
✅ 收集到30个视频

🔬 步骤3：事件2 - 模式分析
✅ 发现15个模式

📝 步骤4：事件3 - 模板生成
✅ 研究报告已生成

============================================================
✅ 执行完成!
============================================================
```

## 第六步：查看结果

```bash
# 查看输出文件
ls -la output/quick_start/

# 查看研究报告
cat output/quick_start/research_report.md

# 查看内容创作指南
cat output/quick_start/content_guide.md
```

---

## RCSW 工作流

遵循认知敏捷法的四阶段工作流：

```
Real (现实约束) → Cog (认知模型) → Spec (规约文档) → Work (实际作品)
```

### 1. Real - 查看现实约束
编辑 `.42cog/real/real.md`，了解项目的现实约束：
```bash
cat .42cog/real/real.md
```

### 2. Cog - 理解认知模型
查看 `.42cog/cog/cog.md`，了解核心实体：
```bash
cat .42cog/cog/cog.md
```

### 3. Spec - 浏览规约文档
```bash
# 查看用户视角规约
cat .42cog/spec/user/workflow.spec.md

# 查看产品视角规约
cat .42cog/spec/pm/template.spec.md

# 查看技术视角规约
cat .42cog/spec/dev/data.spec.md
```

### 4. Work - 查看实际作品
```bash
# 查看源代码结构
tree src/ -L 2

# 查看核心模块
ls -la src/research/
ls -la src/analysis/
ls -la src/template/
```

---

## 示例脚本说明

### 1. 基础示例
```bash
python3 run.py quick
# 或
python3 examples/quick_start.py
```

### 2. 自定义分析
```bash
python3 run.py custom
# 或
python3 examples/custom_analysis.py
```

### 3. 批量分析
```bash
python3 run.py batch
# 或
python3 examples/batch_analysis.py
```

### 4. 动态追踪
```bash
python3 examples/dynamic_tracking.py
```

### 5. MCP集成
```bash
python3 run.py mcp
# 或
python3 examples/mcp_integration.py
```

---

## 目录说明

```
youtube 视频的最小故事/
├── .42cog/                   # 42COG框架核心文件
│   ├── real/                 # 现实约束
│   ├── cog/                  # 认知模型
│   ├── spec/                 # 规约文档
│   └── work/                 # 工作记录
├── src/                      # 源代码（代码生成目录）⭐
│   ├── research/             # 数据收集模块
│   ├── analysis/             # 模式分析模块
│   ├── template/             # 模板生成模块
│   ├── workflow/             # 工作流管理模块
│   ├── monitoring/           # 动态追踪模块
│   └── utils/                # 工具模块
├── config/                   # 配置文件
├── examples/                 # 示例脚本
├── output/                   # 输出目录（运行时生成）
├── logs/                     # 日志目录（运行时生成）
├── .env.example              # 环境变量模板
├── requirements.txt          # 依赖包列表
├── run.py                    # 统一启动脚本
├── README.md                # 项目说明
├── 快速开始.md              # 本文档
├── 使用指南.md              # 详细使用指南
└── PROJECT_SUMMARY.md       # 项目总结
```

---

## 常见问题

### Q1: 本项目需要YouTube API密钥吗？
**A**: 不需要！本项目使用MCP fetch进行真实网页浏览，无需申请或配置YouTube API密钥。

### Q2: MCP fetch如何获取数据？
**A**: 确保CC-Switch正在运行，在Claude Code中调用MCP工具：`@fetch 获取 https://www.youtube.com/results?search_query=Python教程`

### Q3: 模式分析结果为空？
**A**:
- 降低 `min_pattern_frequency`（如设为2）
- 增加视频数量
- 检查数据质量和编码

---

## 下一步

1. **理解框架**：阅读 `.42cog/README.md` 了解42COG方法论
2. **查看约束**：浏览 `.42cog/real/real.md` 了解现实约束
3. **学习模型**：研究 `.42cog/cog/cog.md` 理解认知模型
4. **运行示例**：执行 `python3 run.py quick` 体验完整流程
5. **自定义配置**：根据需要修改 `config/config.yaml`
6. **深入开发**：查看 `src/` 目录下的源代码实现

---

**开始您的YouTube视频研究之旅！** 🚀

*最后更新：2025-12-09*
