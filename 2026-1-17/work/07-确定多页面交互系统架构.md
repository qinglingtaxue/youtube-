# TD-07: 多页面交互系统架构 / Multi-page Interactive System Architecture

> 基于 Next.js App Router 的多页面应用结构设计
> Multi-page application structure design based on Next.js App Router

---

## 决策概要 / Decision Summary

| 属性 / Attribute | 值 / Value |
|------------------|------------|
| 决策编号 / ID | TD-07 |
| 决策类型 / Type | 应用架构 / Application Architecture |
| 决策日期 / Date | 2026-01-19 |
| 来源对话 / Source | fbd8379a |
| 状态 / Status | 已执行 / Implemented |

---

## 应用结构 / Application Structure

```
src/
├── app/                              # Next.js App Router
│   ├── layout.tsx                    # 根布局 / Root Layout
│   ├── page.tsx                      # 首页 / Homepage
│   ├── (dashboard)/                  # 仪表盘分组 / Dashboard Group
│   │   ├── layout.tsx                # 仪表盘布局
│   │   ├── videos/                   # 视频模块 / Videos Module
│   │   │   ├── page.tsx              # 视频列表
│   │   │   ├── [id]/page.tsx         # 视频详情
│   │   │   ├── trending/page.tsx     # 热门视频
│   │   │   ├── viral/page.tsx        # 爆款视频
│   │   │   └── potential/page.tsx    # 潜力视频
│   │   ├── channels/                 # 频道模块 / Channels Module
│   │   │   ├── page.tsx              # 频道列表
│   │   │   └── [id]/page.tsx         # 频道详情
│   │   ├── analysis/                 # 分析模块 / Analysis Module
│   │   │   ├── page.tsx              # 数据分析
│   │   │   └── trends/page.tsx       # 趋势分析
│   │   └── collect/                  # 采集模块 / Collection Module
│   │       ├── page.tsx              # 采集任务
│   │       └── history/page.tsx      # 历史记录
│   └── api/                          # API 路由 / API Routes
│       ├── videos/route.ts
│       ├── channels/route.ts
│       └── collect/route.ts
├── components/                       # 组件 / Components
│   ├── layout/                       # 布局组件
│   │   ├── Header.tsx
│   │   ├── Sidebar.tsx
│   │   └── Footer.tsx
│   ├── ui/                           # 通用 UI 组件
│   │   ├── Button.tsx
│   │   ├── Card.tsx
│   │   ├── Table.tsx
│   │   └── ...
│   ├── video/                        # 视频相关组件
│   │   ├── VideoCard.tsx
│   │   ├── VideoList.tsx
│   │   └── VideoFilters.tsx
│   ├── channel/                      # 频道相关组件
│   └── chart/                        # 图表组件
│       ├── TrendChart.tsx
│       └── PieChart.tsx
├── lib/                              # 工具库 / Utilities
│   ├── data.ts                       # 数据获取
│   ├── utils.ts                      # 工具函数
│   └── constants.ts                  # 常量
└── types/                            # 类型定义 / Types
    ├── video.ts
    └── channel.ts
```

---

## 页面索引 / Page Index

| # | 路由 / Route | 中文名称 | English Name | 功能 / Function |
|---|--------------|---------|--------------|-----------------|
| 1 | `/` | 首页 | Homepage | 数据概览仪表盘 |
| 2 | `/videos` | 视频榜单 | Video Rankings | 全部视频列表 |
| 3 | `/videos/trending` | 热门视频 | Trending Videos | 当前热门榜 |
| 4 | `/videos/viral` | 爆款视频 | Viral Videos | 爆款 Top 50 |
| 5 | `/videos/potential` | 潜力视频 | Potential Videos | 增长潜力榜 |
| 6 | `/videos/[id]` | 视频详情 | Video Detail | 单个视频详情 |
| 7 | `/channels` | 频道榜单 | Channel Rankings | 频道列表 |
| 8 | `/channels/[id]` | 频道详情 | Channel Detail | 单个频道详情 |
| 9 | `/analysis` | 数据分析 | Data Analysis | 图表分析 |
| 10 | `/collect` | 采集管理 | Collection Manager | 任务配置执行 |

---

## 架构详解 / Architecture Details

### 1. 页面层级 / Page Hierarchy

```
                              ┌─────────────────┐
                              │    Root Layout  │
                              │   (Header, Nav) │
                              └────────┬────────┘
                                       │
              ┌────────────────────────┼────────────────────────┐
              │                        │                        │
              ▼                        ▼                        ▼
      ┌───────────────┐       ┌───────────────┐       ┌───────────────┐
      │   Homepage    │       │   Dashboard   │       │    API        │
      │      /        │       │    Layout     │       │   Routes      │
      └───────────────┘       └───────┬───────┘       └───────────────┘
                                      │
              ┌───────────┬───────────┼───────────┬───────────┐
              │           │           │           │           │
              ▼           ▼           ▼           ▼           ▼
        ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐
        │ Videos  │ │Channels │ │Analysis │ │ Collect │ │Settings │
        │ /videos │ │/channels│ │/analysis│ │/collect │ │/settings│
        └────┬────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘
             │
    ┌────────┼────────┬────────┐
    │        │        │        │
    ▼        ▼        ▼        ▼
 trending  viral  potential  [id]
```

---

### 2. 布局组件 / Layout Components

```tsx
// app/layout.tsx - 根布局
import { Header } from '@/components/layout/Header'

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="zh-CN">
      <body className="min-h-screen bg-gray-50">
        <Header />
        <main className="container mx-auto px-4 py-8">
          {children}
        </main>
      </body>
    </html>
  )
}
```

```tsx
// app/(dashboard)/layout.tsx - 仪表盘布局
import { Sidebar } from '@/components/layout/Sidebar'

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <div className="flex">
      <Sidebar />
      <div className="flex-1 p-6">
        {children}
      </div>
    </div>
  )
}
```

```tsx
// components/layout/Sidebar.tsx
'use client'

import Link from 'next/link'
import { usePathname } from 'next/navigation'
import {
  Video,
  Users,
  BarChart3,
  Download,
  Settings,
} from 'lucide-react'

const navItems = [
  { href: '/videos', label: '视频榜单', icon: Video },
  { href: '/channels', label: '频道榜单', icon: Users },
  { href: '/analysis', label: '数据分析', icon: BarChart3 },
  { href: '/collect', label: '采集管理', icon: Download },
  { href: '/settings', label: '系统设置', icon: Settings },
]

export function Sidebar() {
  const pathname = usePathname()

  return (
    <aside className="w-64 bg-white border-r min-h-screen">
      <nav className="p-4 space-y-2">
        {navItems.map((item) => {
          const Icon = item.icon
          const isActive = pathname.startsWith(item.href)

          return (
            <Link
              key={item.href}
              href={item.href}
              className={`
                flex items-center gap-3 px-4 py-3 rounded-lg
                transition-colors
                ${isActive
                  ? 'bg-blue-50 text-blue-600'
                  : 'text-gray-600 hover:bg-gray-50'
                }
              `}
            >
              <Icon className="w-5 h-5" />
              <span>{item.label}</span>
            </Link>
          )
        })}
      </nav>
    </aside>
  )
}
```

---

### 3. 页面组件 / Page Components

```tsx
// app/(dashboard)/videos/page.tsx
import { Suspense } from 'react'
import { VideoList } from '@/components/video/VideoList'
import { VideoFilters } from '@/components/video/VideoFilters'
import { Tabs, TabsList, TabsTrigger } from '@/components/ui/Tabs'
import { getVideos } from '@/lib/data'

interface Props {
  searchParams: {
    filter?: string
    sort?: string
    page?: string
  }
}

export default async function VideosPage({ searchParams }: Props) {
  const filter = searchParams.filter || 'all'
  const sort = searchParams.sort || 'views'
  const page = parseInt(searchParams.page || '1')

  return (
    <div className="space-y-6">
      {/* 页面标题 */}
      <div className="flex items-center justify-between">
        <h1 className="text-2xl font-bold">视频榜单</h1>
        <VideoFilters />
      </div>

      {/* 分类标签 */}
      <Tabs defaultValue={filter}>
        <TabsList>
          <TabsTrigger value="all" href="/videos">全部</TabsTrigger>
          <TabsTrigger value="trending" href="/videos/trending">热门</TabsTrigger>
          <TabsTrigger value="viral" href="/videos/viral">爆款</TabsTrigger>
          <TabsTrigger value="potential" href="/videos/potential">潜力</TabsTrigger>
        </TabsList>
      </Tabs>

      {/* 视频列表 */}
      <Suspense fallback={<VideoListSkeleton />}>
        <VideoListWrapper filter={filter} sort={sort} page={page} />
      </Suspense>
    </div>
  )
}

async function VideoListWrapper({
  filter,
  sort,
  page,
}: {
  filter: string
  sort: string
  page: number
}) {
  const { videos, total } = await getVideos({ filter, sort, page })

  return (
    <VideoList
      videos={videos}
      total={total}
      currentPage={page}
    />
  )
}
```

---

### 4. 交互设计 / Interaction Design

```
┌─────────────────────────────────────────────────────────────────────┐
│  Header                                               [Search] [👤] │
├──────────────┬──────────────────────────────────────────────────────┤
│              │                                                      │
│  Sidebar     │  视频榜单                                  [筛选] ▼  │
│              │  ─────────────────────────────────────────────────── │
│  📹 视频榜单 │  [全部] [热门] [爆款] [潜力]                          │
│  👥 频道榜单 │  ─────────────────────────────────────────────────── │
│  📊 数据分析 │                                                      │
│  ⬇️ 采集管理 │  ┌──────────────────────────────────────────────────┐ │
│  ⚙️ 系统设置 │  │ 视频卡片 1                                        │ │
│              │  │ 🖼️ [缩略图]  标题...                              │ │
│              │  │              频道名 · 100万次观看 · 2天前          │ │
│              │  └──────────────────────────────────────────────────┘ │
│              │                                                      │
│              │  ┌──────────────────────────────────────────────────┐ │
│              │  │ 视频卡片 2                                        │ │
│              │  │ ...                                              │ │
│              │  └──────────────────────────────────────────────────┘ │
│              │                                                      │
│              │  ─────────────────────────────────────────────────── │
│              │  [< 上一页]  1  2  3  ...  10  [下一页 >]            │
│              │                                                      │
└──────────────┴──────────────────────────────────────────────────────┘
```

**筛选器状态 / Filter State:**

```typescript
// URL 参数持久化筛选状态
// /videos?filter=viral&sort=views&time=7d&page=1

interface FilterState {
  filter: 'all' | 'trending' | 'viral' | 'potential'
  sort: 'views' | 'date' | 'engagement'
  time: '24h' | '7d' | '30d' | '90d'
  page: number
}

// 使用 URL 参数管理状态
function useFilters(): [FilterState, (filters: Partial<FilterState>) => void] {
  const searchParams = useSearchParams()
  const router = useRouter()
  const pathname = usePathname()

  const filters: FilterState = {
    filter: (searchParams.get('filter') as FilterState['filter']) || 'all',
    sort: (searchParams.get('sort') as FilterState['sort']) || 'views',
    time: (searchParams.get('time') as FilterState['time']) || '7d',
    page: parseInt(searchParams.get('page') || '1'),
  }

  const setFilters = (newFilters: Partial<FilterState>) => {
    const params = new URLSearchParams(searchParams)
    Object.entries(newFilters).forEach(([key, value]) => {
      params.set(key, String(value))
    })
    router.push(`${pathname}?${params.toString()}`)
  }

  return [filters, setFilters]
}
```

---

## 快速导航 / Quick Navigation

### 想了解技术栈？ / Want tech stack?
→ 阅读 [TD-06: Next.js 技术栈](./06-选择NextJS作为前端技术栈.md)

### 想了解视频分类？ / Want video categories?
→ 阅读 [08-定义爆款潜力热门视频标签体系.md](./08-定义爆款潜力热门视频标签体系.md)

### 想了解筛选维度？ / Want filter dimensions?
→ 阅读 [09-设计视频频道榜单筛选维度.md](./09-设计视频频道榜单筛选维度.md)

---

## 设计原则 / Design Principles

1. **URL 语义化 / Semantic URLs**
   - 路由结构反映功能层级
   - 支持深度链接和分享

2. **状态持久化 / State Persistence**
   - 筛选条件保存在 URL
   - 刷新页面不丢失状态

3. **渐进式加载 / Progressive Loading**
   - Suspense 边界处理加载
   - 骨架屏提升感知性能

4. **响应式设计 / Responsive Design**
   - 移动端侧边栏收起
   - 表格适配小屏幕

---

## 注意事项 / Notes

1. **路由分组 / Route Groups**：使用 `(dashboard)` 分组共享布局
2. **动态路由 / Dynamic Routes**：`[id]` 处理详情页
3. **并行路由 / Parallel Routes**：复杂布局可用 `@slot`
4. **拦截路由 / Intercepting**：模态框可用 `(.)` 语法
5. **加载状态 / Loading**：每个路由可配 `loading.tsx`
6. **错误边界 / Error Boundary**：每个路由可配 `error.tsx`

---

## 相关决策 / Related Decisions

| 决策 / Decision | 关系 / Relation |
|-----------------|-----------------|
| [TD-06](./06-选择NextJS作为前端技术栈.md) | 上游：技术基础 |
| [TD-14](./14-增加进度预估与网络速度显示功能.md) | 应用：采集页面 UI |

---

*最后更新 / Last Updated: 2026-01-20*
