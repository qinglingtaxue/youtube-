# YouTube 视频工作流 - UI 设计规格

## 一、概述

基于 [YouTube 工作流技能](youtube_workflow_skill.md) 设计的可视化操作界面，采用 Next.js + Tailwind CSS + shadcn/ui 技术栈。

---

## 二、界面结构

### 2.1 整体布局

```
┌─────────────────────────────────────────────────────────────┐
│  Header: YouTube 视频工作流                            [设置] │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐  │
│  │ 1.下载  │ → │ 2.准备  │ → │ 3.上传  │ → │ 4.清理  │  │
│  │  源     │    │  文件   │    │  目标   │    │  完成   │  │
│  └─────────┘    └─────────┘    └─────────┘    └─────────┘  │
│       ●              ○              ○              ○        │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                                                     │   │
│  │                   当前步骤内容区                      │   │
│  │                                                     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  任务进度: ████████░░░░░░░░░░░░  5/11 视频已完成     │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│  [上一步]                                      [下一步/执行] │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 布局代码

```tsx
// src/app/youtube-workflow/page.tsx
export default function YouTubeWorkflowPage() {
  return (
    <div className="min-h-screen flex flex-col bg-slate-900">
      {/* Header */}
      <header className="bg-slate-800 border-b border-slate-700 px-6 py-4">
        <div className="flex items-center justify-between max-w-6xl mx-auto">
          <h1 className="text-xl font-bold text-white">YouTube 视频工作流</h1>
          <Button variant="ghost" size="icon">
            <Settings className="h-5 w-5 text-slate-400" />
          </Button>
        </div>
      </header>

      {/* Main Content */}
      <main className="flex-1 px-6 py-8">
        <div className="max-w-6xl mx-auto space-y-8">
          {/* Step Indicator */}
          <StepIndicator currentStep={1} />

          {/* Step Content */}
          <div className="bg-slate-800/50 rounded-xl p-6 min-h-[400px]">
            <StepContent step={1} />
          </div>

          {/* Progress Bar */}
          <ProgressBar current={5} total={11} />
        </div>
      </main>

      {/* Footer Navigation */}
      <footer className="bg-slate-800 border-t border-slate-700 px-6 py-4">
        <div className="flex items-center justify-between max-w-6xl mx-auto">
          <Button variant="outline">上一步</Button>
          <Button>下一步</Button>
        </div>
      </footer>
    </div>
  )
}
```

---

## 三、步骤 1: 下载源选择

### 3.1 界面设计

```
┌─────────────────────────────────────────────────────────────┐
│  选择视频来源                                                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐   │
│  │   📺          │  │   📺          │  │   ☁️          │   │
│  │  YouTube      │  │  YouTube      │  │   百度        │   │
│  │  Studio       │  │  公开视频     │  │   网盘        │   │
│  │  (自己频道)    │  │  (yt-dlp)     │  │               │   │
│  │               │  │               │  │               │   │
│  │  [选择]       │  │  [选择]       │  │  [选择]       │   │
│  └───────────────┘  └───────────────┘  └───────────────┘   │
│                                                             │
│  ┌───────────────┐  ┌───────────────┐                      │
│  │   ☁️          │  │   📁          │                      │
│  │   阿里        │  │   本地        │                      │
│  │   云盘        │  │   文件夹      │                      │
│  │               │  │               │                      │
│  │               │  │  (跳过下载)    │                      │
│  │  [选择]       │  │  [选择]       │                      │
│  └───────────────┘  └───────────────┘                      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 组件代码

```tsx
// src/components/workflow/SourceSelector.tsx
const sources = [
  { id: 'youtube-studio', name: 'YouTube Studio', icon: '📺', desc: '下载自己频道的视频' },
  { id: 'youtube-public', name: 'YouTube 公开视频', icon: '📺', desc: '使用 yt-dlp 下载' },
  { id: 'baidu-pan', name: '百度网盘', icon: '☁️', desc: '从网盘下载' },
  { id: 'aliyun-pan', name: '阿里云盘', icon: '☁️', desc: '从网盘下载' },
  { id: 'local', name: '本地文件夹', icon: '📁', desc: '已有视频，跳过下载' },
]

export function SourceSelector({ selected, onSelect }) {
  return (
    <div className="space-y-4">
      <h2 className="text-lg font-semibold text-white">选择视频来源</h2>
      <div className="grid grid-cols-3 gap-4">
        {sources.map((source) => (
          <button
            key={source.id}
            onClick={() => onSelect(source.id)}
            className={cn(
              "p-6 rounded-xl border-2 text-left transition-all",
              "hover:border-cyan-500 hover:bg-slate-800",
              selected === source.id
                ? "border-cyan-500 bg-slate-800"
                : "border-slate-700 bg-slate-800/50"
            )}
          >
            <span className="text-3xl">{source.icon}</span>
            <h3 className="mt-3 font-medium text-white">{source.name}</h3>
            <p className="mt-1 text-sm text-slate-400">{source.desc}</p>
          </button>
        ))}
      </div>
    </div>
  )
}
```

### 3.3 源信息表单

根据选择的来源显示不同的表单：

```tsx
// YouTube Studio 表单
<div className="space-y-4 mt-6 p-4 bg-slate-800 rounded-lg">
  <div>
    <Label className="text-slate-200">播放列表 URL</Label>
    <Input
      placeholder="https://studio.youtube.com/playlist/PLxxx/videos"
      className="mt-1 bg-slate-900 border-slate-700 text-white"
    />
  </div>
  <div>
    <Label className="text-slate-200">下载范围</Label>
    <RadioGroup defaultValue="all" className="mt-2">
      <div className="flex items-center space-x-2">
        <RadioGroupItem value="all" id="all" />
        <Label htmlFor="all" className="text-slate-300">全部视频</Label>
      </div>
      <div className="flex items-center space-x-2">
        <RadioGroupItem value="keyword" id="keyword" />
        <Label htmlFor="keyword" className="text-slate-300">按关键词筛选</Label>
      </div>
    </RadioGroup>
  </div>
</div>

// 本地文件夹表单
<div className="space-y-4 mt-6 p-4 bg-slate-800 rounded-lg">
  <div>
    <Label className="text-slate-200">文件夹路径</Label>
    <div className="flex gap-2 mt-1">
      <Input
        value="/Users/su/Downloads/3d_games/上传/"
        className="bg-slate-900 border-slate-700 text-white"
      />
      <Button variant="outline">浏览</Button>
    </div>
  </div>
</div>
```

---

## 四、步骤 2: 文件准备

### 4.1 界面设计

```
┌─────────────────────────────────────────────────────────────┐
│  准备上传文件                                                │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  已找到 11 个视频文件                          [刷新] [全选] │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ ☑ 毛泽东选集-第33集-反对投降活动...     156 MB  ✓  │   │
│  │ ☑ 毛泽东选集-第34集-必须制裁反动派...   142 MB  ✓  │   │
│  │ ☑ 毛泽东选集-第35集-关于国际新形势...   138 MB  ✓  │   │
│  │ ☑ 毛泽东选集-第36集-和中央社...         145 MB  ✓  │   │
│  │ ☐ 毛泽东选集-第37集-苏联利益...         151 MB      │   │
│  │ ...                                                 │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  封面设置                                           │   │
│  │                                                     │   │
│  │  ● 情况1: 不需要封面（使用 YouTube 自动生成）         │   │
│  │  ○ 情况2: 系列视频用同一个封面                       │   │
│  │    ┌──────┐                                        │   │
│  │    │ 🖼️  │  [选择封面图片]                         │   │
│  │    └──────┘                                        │   │
│  │  ○ 情况3: 每个视频用不同封面                         │   │
│  │    封面文件夹: [选择文件夹] (文件名需与视频匹配)      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 文件列表组件

```tsx
// src/components/workflow/FileList.tsx
export function FileList({ files, selected, onToggle }) {
  return (
    <div className="space-y-4">
      <div className="flex items-center justify-between">
        <h2 className="text-lg font-semibold text-white">
          已找到 {files.length} 个视频文件
        </h2>
        <div className="flex gap-2">
          <Button variant="ghost" size="sm">刷新</Button>
          <Button variant="ghost" size="sm">全选</Button>
        </div>
      </div>

      <div className="bg-slate-800 rounded-lg divide-y divide-slate-700 max-h-[300px] overflow-y-auto">
        {files.map((file) => (
          <div key={file.id} className="flex items-center px-4 py-3 hover:bg-slate-700/50">
            <Checkbox
              checked={selected.includes(file.id)}
              onCheckedChange={() => onToggle(file.id)}
            />
            <span className="ml-3 flex-1 text-slate-200 truncate">{file.name}</span>
            <span className="text-sm text-slate-400">{file.size}</span>
            {file.ready && <Check className="ml-2 h-4 w-4 text-green-500" />}
          </div>
        ))}
      </div>
    </div>
  )
}
```

### 4.3 封面设置组件

封面上传分为三种情况：
| 情况 | 适用场景 | 操作 |
|------|----------|------|
| 1. 不需要封面 | 使用 YouTube 自动生成 | 跳过封面设置 |
| 2. 系列视频同封面 | 同一系列的多个视频 | 所有视频使用同一张封面 |
| 3. 独立视频各配封面 | 每个视频是独立内容 | 每个视频配不同封面 |

```tsx
// src/components/workflow/CoverSettings.tsx
type CoverType = 'auto' | 'same' | 'individual'

interface CoverSettingsProps {
  coverType: CoverType
  coverImage: string
  coverFolder: string
  onCoverTypeChange: (type: CoverType) => void
  onCoverImageChange: (path: string) => void
  onCoverFolderChange: (path: string) => void
}

export function CoverSettings({
  coverType,
  coverImage,
  coverFolder,
  onCoverTypeChange,
  onCoverImageChange,
  onCoverFolderChange,
}: CoverSettingsProps) {
  return (
    <div className="p-4 bg-slate-800 rounded-lg space-y-4">
      <h3 className="font-medium text-white">封面设置</h3>

      <RadioGroup value={coverType} onValueChange={onCoverTypeChange}>
        {/* 情况1: 不需要封面 */}
        <div className="flex items-start space-x-2 p-3 rounded-lg hover:bg-slate-700/50">
          <RadioGroupItem value="auto" id="cover-auto" />
          <div>
            <Label htmlFor="cover-auto" className="text-slate-200 font-medium">
              情况1: 不需要封面
            </Label>
            <p className="text-sm text-slate-400">使用 YouTube 自动生成的封面</p>
          </div>
        </div>

        {/* 情况2: 系列视频同封面 */}
        <div className="flex items-start space-x-2 p-3 rounded-lg hover:bg-slate-700/50">
          <RadioGroupItem value="same" id="cover-same" />
          <div className="flex-1">
            <Label htmlFor="cover-same" className="text-slate-200 font-medium">
              情况2: 系列视频用同一个封面
            </Label>
            <p className="text-sm text-slate-400 mb-2">所有视频使用同一张封面图片</p>

            {coverType === 'same' && (
              <div className="flex gap-2 mt-2">
                <Input
                  value={coverImage}
                  onChange={(e) => onCoverImageChange(e.target.value)}
                  placeholder="封面图片路径"
                  className="bg-slate-900 border-slate-700 text-white"
                />
                <Button variant="outline" size="sm">浏览</Button>
              </div>
            )}
          </div>
        </div>

        {/* 情况3: 每个视频用不同封面 */}
        <div className="flex items-start space-x-2 p-3 rounded-lg hover:bg-slate-700/50">
          <RadioGroupItem value="individual" id="cover-individual" />
          <div className="flex-1">
            <Label htmlFor="cover-individual" className="text-slate-200 font-medium">
              情况3: 每个视频用不同封面
            </Label>
            <p className="text-sm text-slate-400 mb-2">
              每个视频配不同封面（文件名需与视频匹配）
            </p>

            {coverType === 'individual' && (
              <div className="flex gap-2 mt-2">
                <Input
                  value={coverFolder}
                  onChange={(e) => onCoverFolderChange(e.target.value)}
                  placeholder="封面文件夹路径"
                  className="bg-slate-900 border-slate-700 text-white"
                />
                <Button variant="outline" size="sm">浏览</Button>
              </div>
            )}
          </div>
        </div>
      </RadioGroup>
    </div>
  )
}
```

---

## 五、步骤 3: 上传设置

### 5.1 界面设计

```
┌─────────────────────────────────────────────────────────────┐
│  上传设置                                                    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  目标频道                                                    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Chrome Profile: [Profile 3          ▼]             │   │
│  │  频道名称: 密友小屋🛖                                │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  播放列表                                                    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  [毛泽东全集                          ▼]            │   │
│  │  □ 创建新播放列表                                    │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  盈利设置                                                    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  ● 开启盈利   ○ 关闭盈利                            │   │
│  │  广告适宜性: [None of the above (默认)    ▼]        │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  定时发布                                                    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  ☑ 启用定时发布                                      │   │
│  │  开始日期: [2026-01-06]  开始时间: [21:00]          │   │
│  │  发布间隔: [15] 分钟                                 │   │
│  │                                                     │   │
│  │  预览:                                               │   │
│  │  · 视频1: 2026-01-06 21:00                          │   │
│  │  · 视频2: 2026-01-06 21:15                          │   │
│  │  · 视频3: 2026-01-06 21:30                          │   │
│  │  · ...                                              │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 上传设置组件

```tsx
// src/components/workflow/UploadSettings.tsx
export function UploadSettings({ settings, onChange }) {
  return (
    <div className="space-y-6">
      {/* 目标频道 */}
      <div className="p-4 bg-slate-800 rounded-lg space-y-3">
        <h3 className="font-medium text-white">目标频道</h3>
        <div className="grid grid-cols-2 gap-4">
          <div>
            <Label className="text-slate-300">Chrome Profile</Label>
            <Select value={settings.profile} onValueChange={(v) => onChange('profile', v)}>
              <SelectTrigger className="mt-1 bg-slate-900 border-slate-700">
                <SelectValue />
              </SelectTrigger>
              <SelectContent>
                <SelectItem value="Default">Default</SelectItem>
                <SelectItem value="Profile 1">Profile 1</SelectItem>
                <SelectItem value="Profile 3">Profile 3</SelectItem>
              </SelectContent>
            </Select>
          </div>
          <div>
            <Label className="text-slate-300">频道名称</Label>
            <p className="mt-2 text-white">密友小屋🛖</p>
          </div>
        </div>
      </div>

      {/* 播放列表 */}
      <div className="p-4 bg-slate-800 rounded-lg space-y-3">
        <h3 className="font-medium text-white">播放列表</h3>
        <Select value={settings.playlist} onValueChange={(v) => onChange('playlist', v)}>
          <SelectTrigger className="bg-slate-900 border-slate-700">
            <SelectValue />
          </SelectTrigger>
          <SelectContent>
            <SelectItem value="毛泽东全集">毛泽东全集</SelectItem>
            <SelectItem value="其他播放列表">其他播放列表</SelectItem>
          </SelectContent>
        </Select>
      </div>

      {/* 盈利设置 */}
      <div className="p-4 bg-slate-800 rounded-lg space-y-3">
        <h3 className="font-medium text-white">盈利设置</h3>
        <RadioGroup
          value={settings.monetization ? 'on' : 'off'}
          onValueChange={(v) => onChange('monetization', v === 'on')}
          className="flex gap-6"
        >
          <div className="flex items-center space-x-2">
            <RadioGroupItem value="on" id="mon-on" />
            <Label htmlFor="mon-on" className="text-slate-300">开启盈利</Label>
          </div>
          <div className="flex items-center space-x-2">
            <RadioGroupItem value="off" id="mon-off" />
            <Label htmlFor="mon-off" className="text-slate-300">关闭盈利</Label>
          </div>
        </RadioGroup>
        <div>
          <Label className="text-slate-300">广告适宜性</Label>
          <Select defaultValue="none">
            <SelectTrigger className="mt-1 bg-slate-900 border-slate-700">
              <SelectValue />
            </SelectTrigger>
            <SelectContent>
              <SelectItem value="none">None of the above (默认)</SelectItem>
            </SelectContent>
          </Select>
        </div>
      </div>

      {/* 定时发布 */}
      <div className="p-4 bg-slate-800 rounded-lg space-y-3">
        <div className="flex items-center gap-2">
          <Checkbox
            checked={settings.scheduled}
            onCheckedChange={(v) => onChange('scheduled', v)}
          />
          <h3 className="font-medium text-white">启用定时发布</h3>
        </div>

        {settings.scheduled && (
          <div className="space-y-4 mt-4">
            <div className="grid grid-cols-3 gap-4">
              <div>
                <Label className="text-slate-300">开始日期</Label>
                <Input
                  type="date"
                  value={settings.startDate}
                  onChange={(e) => onChange('startDate', e.target.value)}
                  className="mt-1 bg-slate-900 border-slate-700"
                />
              </div>
              <div>
                <Label className="text-slate-300">开始时间</Label>
                <Input
                  type="time"
                  value={settings.startTime}
                  onChange={(e) => onChange('startTime', e.target.value)}
                  className="mt-1 bg-slate-900 border-slate-700"
                />
              </div>
              <div>
                <Label className="text-slate-300">发布间隔(分钟)</Label>
                <Input
                  type="number"
                  value={settings.interval}
                  onChange={(e) => onChange('interval', e.target.value)}
                  className="mt-1 bg-slate-900 border-slate-700"
                />
              </div>
            </div>

            {/* 时间计算规则提示 */}
            <div className="mt-4 p-3 bg-amber-900/30 border border-amber-700/50 rounded text-sm">
              <p className="font-medium text-amber-300 mb-1">时间计算规则：</p>
              <ul className="text-amber-200/80 space-y-1">
                <li>· 第一个视频 = 当前时间 + 5-10 分钟（预留缓冲）</li>
                <li>· 使用当前日期，不设置到未来日期</li>
                <li>· 后续视频间隔 13-16 分钟</li>
                <li>· 时间过期时：当前及后续视频时间都往后推</li>
              </ul>
            </div>

            {/* 时间预览 */}
            <div className="mt-4 p-3 bg-slate-900 rounded text-sm text-slate-400">
              <p className="font-medium text-slate-300 mb-2">发布时间预览:</p>
              <ul className="space-y-1">
                <li>· 视频1: {currentDate} {nearestTime}（当前时间 + 缓冲）</li>
                <li>· 视频2: {currentDate} {nearestTime + 15min}</li>
                <li>· 视频3: {currentDate} {nearestTime + 30min}</li>
              </ul>
            </div>
          </div>
        )}
      </div>
    </div>
  )
}
```

---

## 六、步骤 4: 执行与清理

### 6.1 执行中界面

```
┌─────────────────────────────────────────────────────────────┐
│  正在上传...                                                 │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  当前: 毛泽东选集-第35集-关于国际新形势...                    │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  ████████████████░░░░░░░░░░░░░░░░░░  45%            │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  ✓ 第33集 - 已定时 21:00                            │   │
│  │  ✓ 第34集 - 已定时 21:15                            │   │
│  │  ⟳ 第35集 - 上传中... 设置封面                       │   │
│  │  ○ 第36集 - 等待中                                  │   │
│  │  ○ 第37集 - 等待中                                  │   │
│  │  ...                                                │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  操作日志:                                                   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  [21:05:32] 上传封面图片...                          │   │
│  │  [21:05:30] 选择播放列表: 毛泽东全集                  │   │
│  │  [21:05:28] 开始上传第35集                           │   │
│  │  [21:05:15] 第34集定时发布成功                       │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│                                              [暂停] [取消]  │
└─────────────────────────────────────────────────────────────┘
```

### 6.2 完成界面

```
┌─────────────────────────────────────────────────────────────┐
│  ✓ 上传完成                                                  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │                        🎉                             │ │
│  │                                                       │ │
│  │              成功上传 11 个视频                        │ │
│  │              全部已设置为定时发布                       │ │
│  │                                                       │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                             │
│  清理设置                                                    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  ● 删除本地视频文件                                   │   │
│  │  ○ 保留本地视频文件                                   │   │
│  │  ○ 移动到归档文件夹                                   │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  验收清单                                                    │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  ☑ 所有视频状态为 Scheduled                          │   │
│  │  ☑ 封面图片已上传                                    │   │
│  │  ☑ 播放列表已选择                                    │   │
│  │  ☑ 盈利已开启                                        │   │
│  │  ☑ 广告适宜性已提交                                  │   │
│  │  ☑ 未触发验证码                                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│                                    [查看 YouTube Studio]    │
│                                    [执行清理]              │
└─────────────────────────────────────────────────────────────┘
```

---

## 七、颜色规范

### 7.1 配色方案

| 用途 | 颜色 | Tailwind 类 |
|------|------|-------------|
| 背景(主) | #0f172a | `bg-slate-900` |
| 背景(卡片) | #1e293b | `bg-slate-800` |
| 边框 | #334155 | `border-slate-700` |
| 文字(主要) | #ffffff | `text-white` |
| 文字(次要) | #94a3b8 | `text-slate-400` |
| 强调色 | #22d3ee | `text-cyan-400` |
| 成功 | #22c55e | `text-green-500` |
| 警告 | #f59e0b | `text-amber-500` |
| 错误 | #ef4444 | `text-red-500` |

### 7.2 对比度验证

| 组合 | 对比度 | 是否合规 |
|------|--------|---------|
| 白色文字 on slate-900 | 15.8:1 | ✅ AAA |
| slate-400 on slate-900 | 5.4:1 | ✅ AA |
| cyan-400 on slate-800 | 7.2:1 | ✅ AAA |

---

## 八、响应式设计

### 8.1 断点

| 断点 | 宽度 | 布局调整 |
|------|------|---------|
| 移动端 | < 640px | 单列布局，步骤指示器纵向 |
| 平板端 | 640-1024px | 两列布局 |
| 桌面端 | > 1024px | 三列布局，完整显示 |

### 8.2 移动端适配

```tsx
// 步骤指示器 - 移动端纵向
<div className="flex flex-col sm:flex-row gap-2 sm:gap-4">
  {steps.map((step) => (
    <StepItem key={step.id} {...step} />
  ))}
</div>

// 来源选择 - 移动端两列
<div className="grid grid-cols-2 sm:grid-cols-3 gap-3 sm:gap-4">
  {sources.map((source) => (
    <SourceCard key={source.id} {...source} />
  ))}
</div>
```

---

## 九、交互状态

### 9.1 按钮状态

```tsx
// 主按钮
<Button className="bg-cyan-600 hover:bg-cyan-500 text-white">
  下一步
</Button>

// 禁用状态
<Button disabled className="bg-slate-700 text-slate-500 cursor-not-allowed">
  下一步
</Button>

// 加载状态
<Button disabled className="bg-cyan-600">
  <Loader2 className="mr-2 h-4 w-4 animate-spin" />
  处理中...
</Button>
```

### 9.2 卡片选中状态

```tsx
<div className={cn(
  "p-4 rounded-lg border-2 transition-all cursor-pointer",
  selected
    ? "border-cyan-500 bg-slate-800 ring-2 ring-cyan-500/20"
    : "border-slate-700 bg-slate-800/50 hover:border-slate-600"
)}>
```

---

## 十、数据模型

### 10.1 核心实体

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│    Workflow     │────<│   WorkflowTask  │────<│    TaskLog      │
│    (工作流)      │     │   (任务/视频)    │     │   (操作日志)    │
└─────────────────┘     └─────────────────┘     └─────────────────┘
        │
        │
        ▼
┌─────────────────┐
│ WorkflowConfig  │
│   (配置模板)     │
└─────────────────┘
```

### 10.2 数据库 Schema - EAV 模式 (Drizzle ORM)

采用 EAV（Entity-Attribute-Value）模式，将实体、属性、类型、值分离：

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   entities   │────<│  attributes  │     │    types     │
│   (实体)      │     │   (属性)      │────>│   (类型)     │
└──────────────┘     └──────────────┘     └──────────────┘
                            │
                            ▼
                     ┌──────────────┐
                     │    values    │
                     │    (值)      │
                     └──────────────┘
```

```typescript
// src/lib/db/schema/eav.ts
import { pgTable, text, timestamp, integer, boolean, index } from 'drizzle-orm/pg-core'

// 实体表 - 存储所有实体（工作流、任务、配置等）
export const entities = pgTable('entities', {
  id: text('id').primaryKey(),
  type: text('type').notNull(),           // workflow | task | config | log
  name: text('name'),                      // 可选的显示名称
  parentId: text('parent_id'),             // 父实体 ID（如 task 属于 workflow）
  status: text('status'),                  // 通用状态字段
  createdAt: timestamp('created_at').defaultNow(),
  updatedAt: timestamp('updated_at').defaultNow(),
}, (table) => ({
  typeIdx: index('entities_type_idx').on(table.type),
  parentIdx: index('entities_parent_idx').on(table.parentId),
}))

// 类型表 - 定义属性的数据类型
export const types = pgTable('types', {
  id: text('id').primaryKey(),             // string | number | boolean | date | json
  name: text('name').notNull(),            // 类型名称
  description: text('description'),        // 类型描述
})

// 属性表 - 定义可用的属性
export const attributes = pgTable('attributes', {
  id: text('id').primaryKey(),             // source_type | playlist | monetization 等
  name: text('name').notNull(),            // 属性显示名称
  typeId: text('type_id').references(() => types.id).notNull(),
  entityType: text('entity_type').notNull(), // 属于哪种实体类型
  description: text('description'),        // 属性描述
  defaultValue: text('default_value'),     // 默认值
  required: boolean('required').default(false),
  sortOrder: integer('sort_order').default(0),
}, (table) => ({
  entityTypeIdx: index('attributes_entity_type_idx').on(table.entityType),
}))

// 值表 - 存储实际的属性值
export const values = pgTable('values', {
  id: text('id').primaryKey(),
  entityId: text('entity_id').references(() => entities.id).notNull(),
  attributeId: text('attribute_id').references(() => attributes.id).notNull(),

  // 不同类型的值存储在不同列
  stringValue: text('string_value'),
  numberValue: integer('number_value'),
  booleanValue: boolean('boolean_value'),
  dateValue: timestamp('date_value'),
  jsonValue: text('json_value'),           // JSON 字符串

  createdAt: timestamp('created_at').defaultNow(),
  updatedAt: timestamp('updated_at').defaultNow(),
}, (table) => ({
  entityIdx: index('values_entity_idx').on(table.entityId),
  attrIdx: index('values_attribute_idx').on(table.attributeId),
  entityAttrIdx: index('values_entity_attr_idx').on(table.entityId, table.attributeId),
}))
```

### 10.3 初始化数据

```typescript
// src/lib/db/seed/eav-init.ts

// 类型定义
const types = [
  { id: 'string', name: '字符串' },
  { id: 'number', name: '数字' },
  { id: 'boolean', name: '布尔' },
  { id: 'date', name: '日期时间' },
  { id: 'json', name: 'JSON对象' },
]

// 属性定义
const attributes = [
  // 工作流配置属性
  { id: 'source_type', name: '下载源类型', typeId: 'string', entityType: 'config', defaultValue: 'local' },
  { id: 'source_url', name: '源地址', typeId: 'string', entityType: 'config' },
  { id: 'source_keywords', name: '筛选关键词', typeId: 'string', entityType: 'config' },
  { id: 'local_path', name: '本地路径', typeId: 'string', entityType: 'config', defaultValue: '/Users/su/Downloads/3d_games/上传/' },
  { id: 'chrome_profile', name: 'Chrome Profile', typeId: 'string', entityType: 'config', defaultValue: 'Profile 3' },
  { id: 'playlist', name: '播放列表', typeId: 'string', entityType: 'config' },
  // 标题设置
  { id: 'title_mode', name: '标题模式', typeId: 'string', entityType: 'config', defaultValue: 'filename' },
  // title_mode 可选值: 'filename' (使用文件名) | 'custom' (自定义标题) | 'template' (模板生成)
  { id: 'title_template', name: '标题模板', typeId: 'string', entityType: 'config' },
  // 当 title_mode='template' 时使用，如 "{系列名} 第{集数}集"

  // 封面设置
  { id: 'cover_type', name: '封面类型', typeId: 'string', entityType: 'config', defaultValue: 'auto' },
  // cover_type 可选值: 'auto' (自动生成) | 'same' (系列统一) | 'individual' (各自不同)
  { id: 'cover_image', name: '封面图片路径', typeId: 'string', entityType: 'config' },
  // 当 cover_type='same' 时使用
  { id: 'cover_folder', name: '封面文件夹', typeId: 'string', entityType: 'config' },
  // 当 cover_type='individual' 时使用，文件名需与视频匹配

  // A/B 测试设置
  { id: 'ab_test_enabled', name: '启用A/B测试', typeId: 'boolean', entityType: 'config', defaultValue: 'false' },
  { id: 'ab_test_titles', name: 'A/B测试标题', typeId: 'json', entityType: 'config' },
  // 最多3个备选标题，格式: ["标题1", "标题2", "标题3"]
  { id: 'ab_test_covers', name: 'A/B测试封面', typeId: 'json', entityType: 'config' },
  // 最多3张备选封面，格式: ["/path/cover1.jpg", "/path/cover2.jpg", "/path/cover3.jpg"]

  // 盈利设置
  { id: 'monetization', name: '开启盈利', typeId: 'boolean', entityType: 'config', defaultValue: 'true' },
  { id: 'ad_suitability', name: '广告适宜性', typeId: 'string', entityType: 'config', defaultValue: 'none' },

  // 可见性与发布设置
  { id: 'visibility', name: '可见性', typeId: 'string', entityType: 'config', defaultValue: 'scheduled' },
  // visibility 可选值: 'public' | 'unlisted' | 'private' | 'scheduled'
  { id: 'start_date', name: '开始日期', typeId: 'string', entityType: 'config' },
  // 当 visibility='scheduled' 时使用
  // 重要：默认使用当前日期，不要设置到未来日期
  { id: 'start_time', name: '开始时间', typeId: 'string', entityType: 'config' },
  // 重要：第一个视频时间 = 当前时间 + 5-10 分钟（预留程序运行缓冲）
  { id: 'interval_minutes', name: '发布间隔(分钟)', typeId: 'number', entityType: 'config', defaultValue: '15' },
  // 推荐 13-16 分钟，默认 15 分钟
  { id: 'cleanup_action', name: '清理动作', typeId: 'string', entityType: 'config', defaultValue: 'delete' },

  // 工作流实例属性
  { id: 'current_step', name: '当前步骤', typeId: 'number', entityType: 'workflow', defaultValue: '1' },
  { id: 'total_tasks', name: '总任务数', typeId: 'number', entityType: 'workflow', defaultValue: '0' },
  { id: 'completed_tasks', name: '已完成', typeId: 'number', entityType: 'workflow', defaultValue: '0' },
  { id: 'failed_tasks', name: '失败数', typeId: 'number', entityType: 'workflow', defaultValue: '0' },
  { id: 'started_at', name: '开始时间', typeId: 'date', entityType: 'workflow' },
  { id: 'completed_at', name: '完成时间', typeId: 'date', entityType: 'workflow' },

  // 任务属性
  { id: 'file_name', name: '文件名', typeId: 'string', entityType: 'task', required: true },
  { id: 'file_path', name: '文件路径', typeId: 'string', entityType: 'task' },
  { id: 'file_size', name: '文件大小', typeId: 'number', entityType: 'task' },
  { id: 'title', name: '视频标题', typeId: 'string', entityType: 'task' },
  { id: 'youtube_video_id', name: 'YouTube视频ID', typeId: 'string', entityType: 'task' },
  { id: 'scheduled_time', name: '定时发布时间', typeId: 'date', entityType: 'task' },
  { id: 'error_message', name: '错误信息', typeId: 'string', entityType: 'task' },

  // 日志属性
  { id: 'action', name: '操作类型', typeId: 'string', entityType: 'log', required: true },
  { id: 'message', name: '日志消息', typeId: 'string', entityType: 'log' },
  { id: 'details', name: '详情', typeId: 'json', entityType: 'log' },
]
```

### 10.4 EAV 操作封装

```typescript
// src/lib/db/eav-service.ts

export class EAVService {
  // 创建实体
  async createEntity(type: string, name?: string, parentId?: string): Promise<string> {
    const id = generateId()
    await db.insert(entities).values({ id, type, name, parentId, status: 'pending' })
    return id
  }

  // 设置属性值
  async setValue(entityId: string, attributeId: string, value: any): Promise<void> {
    const attr = await db.query.attributes.findFirst({ where: eq(attributes.id, attributeId) })
    if (!attr) throw new Error(`Attribute ${attributeId} not found`)

    const valueRecord: any = {
      id: generateId(),
      entityId,
      attributeId,
    }

    // 根据类型存储到对应列
    switch (attr.typeId) {
      case 'string': valueRecord.stringValue = String(value); break
      case 'number': valueRecord.numberValue = Number(value); break
      case 'boolean': valueRecord.booleanValue = Boolean(value); break
      case 'date': valueRecord.dateValue = new Date(value); break
      case 'json': valueRecord.jsonValue = JSON.stringify(value); break
    }

    // 使用 upsert
    await db.insert(values)
      .values(valueRecord)
      .onConflictDoUpdate({
        target: [values.entityId, values.attributeId],
        set: valueRecord,
      })
  }

  // 获取属性值
  async getValue(entityId: string, attributeId: string): Promise<any> {
    const result = await db.query.values.findFirst({
      where: and(eq(values.entityId, entityId), eq(values.attributeId, attributeId)),
      with: { attribute: true },
    })
    if (!result) return null

    switch (result.attribute.typeId) {
      case 'string': return result.stringValue
      case 'number': return result.numberValue
      case 'boolean': return result.booleanValue
      case 'date': return result.dateValue
      case 'json': return JSON.parse(result.jsonValue || '{}')
      default: return result.stringValue
    }
  }

  // 获取实体的所有属性值
  async getEntityValues(entityId: string): Promise<Record<string, any>> {
    const results = await db.query.values.findMany({
      where: eq(values.entityId, entityId),
      with: { attribute: true },
    })

    const obj: Record<string, any> = {}
    for (const result of results) {
      obj[result.attributeId] = this.extractValue(result)
    }
    return obj
  }

  // 批量设置属性值
  async setValues(entityId: string, data: Record<string, any>): Promise<void> {
    for (const [key, value] of Object.entries(data)) {
      await this.setValue(entityId, key, value)
    }
  }

  // 查询实体
  async findEntities(type: string, filters?: Record<string, any>): Promise<any[]> {
    let query = db.select().from(entities).where(eq(entities.type, type))

    // 如果有过滤条件，需要 join values 表
    if (filters) {
      // 复杂查询逻辑...
    }

    return await query
  }

  private extractValue(result: any): any {
    switch (result.attribute.typeId) {
      case 'string': return result.stringValue
      case 'number': return result.numberValue
      case 'boolean': return result.booleanValue
      case 'date': return result.dateValue
      case 'json': return JSON.parse(result.jsonValue || '{}')
      default: return result.stringValue
    }
  }
}

export const eavService = new EAVService()
```

### 10.5 使用示例

```typescript
// 创建工作流配置
const configId = await eavService.createEntity('config', '毛泽东选集上传配置')
await eavService.setValues(configId, {
  source_type: 'local',
  local_path: '/Users/su/Downloads/3d_games/上传/',
  chrome_profile: 'Profile 3',
  playlist: '毛泽东全集',
  monetization: true,
  scheduled: true,
  start_time: '21:00',
  interval_minutes: 15,
})

// 创建工作流实例
const workflowId = await eavService.createEntity('workflow', '执行-2026-01-07')
await eavService.setValue(workflowId, 'config_ref', configId)

// 创建任务
const taskId = await eavService.createEntity('task', '第33集', workflowId)
await eavService.setValues(taskId, {
  file_name: '毛泽东选集-第33集.mp4',
  file_size: 156000000,
  status: 'pending',
})

// 读取配置
const config = await eavService.getEntityValues(configId)
console.log(config.playlist) // "毛泽东全集"
```

### 10.6 EAV vs 传统模式对比

| 特性 | EAV 模式 | 传统模式 |
|------|----------|----------|
| 新增属性 | ✅ 只需插入 attributes 表 | ❌ 需要 ALTER TABLE |
| 查询性能 | ❌ 需要多次 JOIN | ✅ 单表查询 |
| 类型安全 | ⚠️ 运行时检查 | ✅ 编译时检查 |
| 灵活性 | ✅ 高 | ❌ 低 |
| 适用场景 | 配置多变、属性不固定 | 结构稳定、性能敏感 |

**本项目推荐 EAV 的原因**：
- 工作流配置项可能经常调整
- 支持不同下载源有不同的配置字段
- 方便用户自定义扩展属性

### 10.7 状态流转

```
工作流状态流转:
pending → downloading → uploading → cleaning → completed
    ↓         ↓            ↓           ↓
  failed    failed       failed      failed
    ↓         ↓            ↓           ↓
  paused    paused       paused      paused

任务状态流转:
pending → downloading → downloaded → uploading → scheduled
    ↓         ↓                          ↓
  failed    failed                     failed
```

### 10.8 是否需要数据库？

| 场景 | 需要数据库 | 说明 |
|------|-----------|------|
| 单次使用 | ❌ 可选 | 配置存内存，执行完即丢弃 |
| 保存配置模板复用 | ✅ 需要 | 持久化常用配置 |
| 查看历史执行记录 | ✅ 需要 | 持久化工作流和任务 |
| 断点续传 | ✅ 需要 | 持久化执行状态 |
| 多设备同步 | ✅ 需要 | 需要远程数据库 |

### 10.9 数据库配置

| 环境 | 数据库 | 连接方式 |
|------|--------|---------|
| 本地开发 | PostgreSQL | `postgresql://localhost:5432/youtube_workflow` |
| 云端生产 | Neon | `postgresql://[user]:[password]@[host].neon.tech/[database]` |

**Neon 配置参考**：https://neon.tech

```typescript
// src/lib/db/index.ts
import { drizzle } from 'drizzle-orm/neon-http'
import { neon } from '@neondatabase/serverless'

// 本地开发使用标准 PostgreSQL
// 云端使用 Neon serverless driver
const sql = neon(process.env.DATABASE_URL!)
export const db = drizzle(sql)
```

```bash
# .env.local (本地开发)
DATABASE_URL=postgresql://postgres:password@localhost:5432/youtube_workflow

# .env.production (云端 Neon)
DATABASE_URL=postgresql://[user]:[password]@[host].neon.tech/youtube_workflow?sslmode=require
```

**推荐配置**：
- 本地开发：PostgreSQL（Docker 或本地安装）
- 云端部署：[Neon](https://neon.tech)（Serverless PostgreSQL，免费额度充足）

---

## 十一、文件结构

```
src/
├── app/
│   └── youtube-workflow/
│       ├── page.tsx              # 主页面
│       └── layout.tsx            # 布局
├── components/
│   └── workflow/
│       ├── StepIndicator.tsx     # 步骤指示器
│       ├── SourceSelector.tsx    # 来源选择
│       ├── FileList.tsx          # 文件列表
│       ├── UploadSettings.tsx    # 上传设置
│       ├── ExecutionProgress.tsx # 执行进度
│       ├── CompletionSummary.tsx # 完成摘要
│       └── ProgressBar.tsx       # 进度条
└── lib/
    └── workflow/
        ├── types.ts              # 类型定义
        ├── actions.ts            # 服务端操作
        └── hooks.ts              # 自定义 hooks
```

---

## 十二、中断恢复功能

工作流执行过程中可能因网络问题、程序崩溃、用户中断等原因中断。根据中断后的恢复方式，分为两种场景：

### 12.0 两种恢复场景

#### 场景一：在原 Claude 会话中重启

**情况**：用户在同一个 Claude Code 会话中说"继续"。

| 特点 | 说明 |
|------|------|
| 浏览器状态 | ✅ 可能仍然打开 |
| 页面状态 | ✅ 可能保持在中断时的位置 |
| 操作 | 先获取 `browser_snapshot` 判断当前状态 |

**恢复流程**：
```
1. browser_snapshot → 获取当前页面状态
2. 判断位置：
   - 在上传对话框 → 继续完成当前视频设置
   - 在 Dashboard → 检查视频列表
   - 浏览器已关闭 → 按场景二处理
3. 继续操作
```

---

#### 场景二：在新 Claude 会话中恢复

**情况**：用户开启了新的 Claude Code 会话。

| 特点 | 说明 |
|------|------|
| 浏览器状态 | ❌ 已关闭（会话结束时关闭） |
| 登录状态 | ✅ 保留在 Playwright MCP 缓存目录 |
| 操作 | 需要重新打开浏览器并导航 |

**恢复流程**：
```
1. browser_navigate → https://studio.youtube.com
2. 导航到 Content 页面
3. 获取已上传视频列表
4. 对比源文件夹，识别：
   - ✅ Scheduled（已完成）
   - ⚠️ Draft（需完成设置）
   - ❌ 未上传
5. 优先处理 Draft 视频
6. 继续上传剩余视频
```

---

#### 两种场景对比

| 对比项 | 场景一（原会话） | 场景二（新会话） |
|--------|-----------------|-----------------|
| 浏览器 | 可能仍打开 | 已关闭 |
| 登录状态 | 保持 | 保持 |
| 页面状态 | 可能保持 | 需重新导航 |
| 首要操作 | `browser_snapshot` | `browser_navigate` |
| 恢复速度 | 快 | 较慢 |

---

### 12.1 恢复检测界面

当检测到有未完成的工作流时，显示恢复提示：

```
┌─────────────────────────────────────────────────────────────┐
│  ⚠️ 检测到未完成的上传任务                                    │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  上次任务: 民间故事2 系列上传                                 │
│  中断时间: 2026-01-07 15:45                                  │
│                                                             │
│  任务状态:                                                   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  ✅ 已完成 (Scheduled): 2 个视频                     │   │
│  │  ⚠️ 草稿 (Draft): 1 个视频                           │   │
│  │  ❌ 未上传: 7 个视频                                  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  ● 继续上次任务 (从草稿视频开始)                       │   │
│  │  ○ 放弃上次任务，重新开始                             │   │
│  │  ○ 仅完成草稿视频设置                                 │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│                                    [取消]    [确认继续]     │
└─────────────────────────────────────────────────────────────┘
```

### 12.2 恢复组件代码

```tsx
// src/components/workflow/RecoveryDialog.tsx
interface RecoveryDialogProps {
  lastSession: {
    name: string
    interruptedAt: Date
    scheduled: number
    drafts: number
    pending: number
  }
  onContinue: (mode: 'continue' | 'restart' | 'drafts-only') => void
  onCancel: () => void
}

export function RecoveryDialog({
  lastSession,
  onContinue,
  onCancel,
}: RecoveryDialogProps) {
  const [mode, setMode] = useState<'continue' | 'restart' | 'drafts-only'>('continue')

  return (
    <Dialog open>
      <DialogContent className="bg-slate-800 border-slate-700">
        <DialogHeader>
          <DialogTitle className="flex items-center gap-2 text-amber-400">
            <AlertTriangle className="h-5 w-5" />
            检测到未完成的上传任务
          </DialogTitle>
        </DialogHeader>

        <div className="space-y-4">
          <div className="text-slate-300">
            <p>上次任务: {lastSession.name}</p>
            <p className="text-sm text-slate-400">
              中断时间: {formatDate(lastSession.interruptedAt)}
            </p>
          </div>

          <div className="bg-slate-900 rounded-lg p-4 space-y-2">
            <div className="flex items-center gap-2">
              <Check className="h-4 w-4 text-green-500" />
              <span className="text-slate-300">已完成 (Scheduled): {lastSession.scheduled} 个视频</span>
            </div>
            <div className="flex items-center gap-2">
              <AlertCircle className="h-4 w-4 text-amber-500" />
              <span className="text-slate-300">草稿 (Draft): {lastSession.drafts} 个视频</span>
            </div>
            <div className="flex items-center gap-2">
              <XCircle className="h-4 w-4 text-red-500" />
              <span className="text-slate-300">未上传: {lastSession.pending} 个视频</span>
            </div>
          </div>

          <RadioGroup value={mode} onValueChange={(v) => setMode(v as typeof mode)}>
            <div className="flex items-center space-x-2 p-3 rounded-lg hover:bg-slate-700/50">
              <RadioGroupItem value="continue" id="continue" />
              <Label htmlFor="continue" className="text-slate-200">
                继续上次任务 (从草稿视频开始)
              </Label>
            </div>
            <div className="flex items-center space-x-2 p-3 rounded-lg hover:bg-slate-700/50">
              <RadioGroupItem value="restart" id="restart" />
              <Label htmlFor="restart" className="text-slate-200">
                放弃上次任务，重新开始
              </Label>
            </div>
            <div className="flex items-center space-x-2 p-3 rounded-lg hover:bg-slate-700/50">
              <RadioGroupItem value="drafts-only" id="drafts-only" />
              <Label htmlFor="drafts-only" className="text-slate-200">
                仅完成草稿视频设置
              </Label>
            </div>
          </RadioGroup>
        </div>

        <DialogFooter>
          <Button variant="ghost" onClick={onCancel}>取消</Button>
          <Button onClick={() => onContinue(mode)}>确认继续</Button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
  )
}
```

### 12.3 恢复状态数据模型

```typescript
// src/lib/db/seed/eav-init.ts 中添加

// 恢复相关属性
const recoveryAttributes = [
  // 工作流恢复状态
  { id: 'is_interrupted', name: '是否中断', typeId: 'boolean', entityType: 'workflow', defaultValue: 'false' },
  { id: 'interrupted_at', name: '中断时间', typeId: 'date', entityType: 'workflow' },
  { id: 'last_completed_task_id', name: '最后完成的任务', typeId: 'string', entityType: 'workflow' },
  { id: 'recovery_mode', name: '恢复模式', typeId: 'string', entityType: 'workflow' },
  // recovery_mode 可选值: 'continue' | 'restart' | 'drafts-only'

  // 任务恢复状态
  { id: 'youtube_status', name: 'YouTube状态', typeId: 'string', entityType: 'task' },
  // youtube_status 可选值: 'scheduled' | 'draft' | 'processing' | 'not_uploaded'
  { id: 'needs_recovery', name: '需要恢复', typeId: 'boolean', entityType: 'task', defaultValue: 'false' },
]
```

### 12.4 恢复时间重算界面

```
┌─────────────────────────────────────────────────────────────┐
│  重新计算发布时间                                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  当前时间: 2026-01-07 16:20                                  │
│                                                             │
│  已有发布计划:                                                │
│  ┌─────────────────────────────────���───────────────────┐   │
│  │  视频1: 15:15 ✅ (已发布)                            │   │
│  │  视频2: 15:30 ✅ (已定时)                            │   │
│  └─────────────────────────────────────────────────���───┘   │
│                                                             │
│  新的发布计划:                                                │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  视频3 (草稿): 16:30 ← 当前时间 + 10分钟              │   │
│  │  视频4: 16:45                                        │   │
│  │  视频5: 17:00                                        │   │
│  │  ...                                                 │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  发布间隔: [15] 分钟                                         │
│                                                             │
│                                    [返回]    [确认并继续]    │
└─────────────────────────────────────────────────────────────┘
```

### 12.5 恢复执行界面

```
┌─────────────────────────────────────────────────────────────┐
│  ⟳ 恢复上传中...                                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  恢复模式: 从草稿视频继续                                     │
│                                                             │
│  ┌─────────────────���───────────────────────────────────┐   │
│  │  ✓ 视频1 - 已发布 15:15                              │   │
│  │  ✓ 视频2 - 已定时 15:30                              │   │
│  │  ⟳ 视频3 - 完成草稿设置中... (盈利设置)               │   │
│  │  ○ 视频4 - 等待上传                                  │   │
│  │  ○ 视频5 - 等待上传                                  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  当前操作: 设置盈利 → On                                      │
│                                                             │
│                                              [暂停] [取消]  │
└─────────────────────────────────────────────────────────────┘
```

---

## 十三、相关技能

- [YouTube 工作流技能](youtube_workflow_skill.md)
- [YouTube 自动上传技能](youtube_auto_upload_skill.md)
- [YouTube Studio 下载技能](youtube_studio_download_skill.md)
